{
  "name": "chrome-bridge",
  "version": "0.2.0",
  "private": true,
  "description": "No-port CDP bridge: Chrome extension (debugger API) + local relay exposing CDP endpoints on 127.0.0.1:9333 (TCP) and \\\\\\\\.\\\\\\\\pipe\\\\\\\\chrome-bridge (named pipe) for agents. Tab management (open/close/activate), self-upgrade, no port visible to agents when pipe is enabled.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/relay.test.mjs test/pipe.test.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}